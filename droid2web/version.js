/**
 * App / release version.
 * Prefer bumping the repo-root `VERSION` file, then run `./scripts/build-release.sh`
 * (or `./build-and-serve.sh`) — that regenerates this file. You can also edit here directly.
 */
export const APP_VERSION = '0.1.0';
